<%= form_for [@user, @posting, @job_application] do |f| %>
<% if @user.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@user.errors.count, "error") %> prohibited
    your profile from being saved:</h2>
    <ul>
      <% @job_application.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end%>

  <p>
    <%= f.label :cover_letter %><br>
    <%= f.text_field :cover_letter %>
  </p>
  <p>
    <%= f.label :resume %><br>
    <%= f.text_field :resume %>
  </p>
  <p>
    <%= f.label :citizen %><br>
    <%= f.text_field :citizen %>
  </p>

  <%= f.submit 'Submit Application', class: "button" %>

  <% end %>
